/*
 * @创建者: yujinjin9@126.com
 * @描述: 系统自定义组件入口
 */
import { type App } from "vue";

export default {
    install: function (vueAppInstance: App, options?: Record<string, any>) {
        // todo: 注册系统自定义组件 - vueAppInstance.component(name, component)
    }
};
